# ── Auth ──────────────────────────────────────────────
SECRET_KEY=change-me-in-production
JWT_SECRET_KEY=103039dfa22f8b2cf8c98042183e620284c460784625e7e4e9ba40d62c0c2a87

# ── PostgreSQL – User-Daten ───────────────────────────
POSTGRES_USER=maggie
POSTGRES_PASSWORD=maggie_secret
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=maggie_users
# Oder als volle URL (überschreibt die Einzelvariablen):
# DATABASE_URL=postgresql+psycopg2://maggie:maggie_secret@localhost:5432/maggie_users

# ── InfluxDB ──────────────────────────────────────────
INFLUX_URL=http://localhost:8086
INFLUX_TOKEN=XZpMXtmrzENrLAQFeaC7YylbjdLre-Y9uDGXu1k6ihtygbdaFaektPeTC22qq1FeiJwi3hJLovMA7k-sCawN4g==
INFLUX_ORG=MAGGIE
INFLUX_BUCKET=MAGGIE_DB

# ── RXSM Telecommand-Uplink (serieller TX, Motor-Steuerung) ──
# Separater Port zum RXSM-(Test-)Modul, über den Telecommands
# (SDC) an den OBC gesendet werden. Leer lassen → zur Laufzeit
# über /api/command/connect wählen.
TC_SERIAL_PORT=/dev/tty.usbserial-AR0KBHRW
TC_SERIAL_BAUD=38400
TC_AUTOSTART=true

